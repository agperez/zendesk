<p id="notice"><%= notice %></p>

<p>
  <strong>Type:</strong>
  <%= @refresh_audit.type %>
</p>

<p>
  <strong>Stamp:</strong>
  <%= @refresh_audit.stamp %>
</p>

<%= link_to 'Edit', edit_refresh_audit_path(@refresh_audit) %> |
<%= link_to 'Back', refresh_audits_path %>
